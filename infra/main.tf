terraform {
  required_version = ">= 1.6"

  required_providers {
    google = {
      source  = "hashicorp/google"
      version = "~> 6.0"
    }
    google-beta = {
      source  = "hashicorp/google-beta"
      version = "~> 6.0"
    }
  }

  # Remote state stored in GCS — bucket created by bootstrap.sh.
  # bootstrap.sh passes -backend-config="bucket=${PROJECT_ID}-jamtheory-terraform-state".
  backend "gcs" {
    prefix = "jamtheory/state"
  }
}

provider "google" {
  project = var.project_id
  region  = var.region
}

provider "google-beta" {
  project = var.project_id
  region  = var.region
}
